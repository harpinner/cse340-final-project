<%- include('partials/header') %>

<header>
  <p>Get in touch — we read every message, eventually</p>
</header>

<main>

  

  <h2 class="section-title">Messages</h2>
  <% if (contacts.length > 0) { %>
    <p class="inv-count"><%= contacts.length %> messages on file.</p>
  <% } else { %>
    <p class="inv-count">No messages on file.</p>
  <% } %>

  <div class="message-list" id="message-history">
    <% contacts.forEach(function(contact) { %>
      <div class="message-card">
        <div class="message-head">
          <div class="message-sender"><%= contact.user_id %></div>
          <div class="message-date"><%= contact.created_at %></div>
        </div>
        <p class="message-body"><%= contact.message %></p>
        <div class="message-sender"><a href="mailto:<%= contact.email %>">Message Back</a></div>
        <div class="message-sender"><a href="/contacts/<%= contact.id %>/delete" class="delete-btn" style="border-color:var(--flag-red); color:var(--flag-red);">Delete</a></div>
      </div>
    <% }); %>
  </div>
</main>


    <div class="message-card">
      <div class="message-head">
        <div class="message-sender">jenny_o</div>
        <div class="message-date">Jun 11, 2026, 4:22 PM</div>
      </div>
      <p class="message-body">Can someone confirm receipt of the paperwork I sent regarding the frame issue? I'd like it on record that I asked nicely first.</p>
    </div>

  </div>

</main>

<footer>
  LARGE MOTORS — mirrors the contacts table (user_id, message, created_at). This page
  has no JavaScript: the form submits natively and, without a real backend attached,
  just reloads the page rather than adding a new message.
</footer>

</body>
</html>