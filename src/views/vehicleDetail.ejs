<%- include('partials/header') %>

<main>

  <div class="crumb"><a href="/vehicles">Inventory</a> </div>

  <!-- vehicles JOIN categories JOIN vehicle_images (id=1) -->
  <div class="detail-hero">

    <div class="photo-gallery">
      <div class="photo-frame"><img src="<%=vehicleImage %>" alt=""></div>
    </div>

    <div class="info-panel">
      <span class="category-badge">Coupe</span>
      <h1>2011 Meridian Coupe</h1>
      <div class="price-line">$2,995.00</div>

      <p class="vehicle-description">
        Runs when warm, cold, or medium. Aftermarket duct-tape trim, matches interior.
        Comes with the good spare, not the donut.
      </p>

      <button class="contact-btn">Ask About This Vehicle</button>
    </div>
  </div>

  <h2 class="section-title">Customer Reviews</h2>

  <!-- reviews JOIN users ON reviews.user_id = users.id WHERE vehicle_id = 1 -->
  <div class="review-summary">
    <div class="review-score">2.0</div>
    <div>
      <div class="review-stars">★★☆☆☆</div>
      <div class="review-count">Based on 6 reviews</div>
    </div>
  </div>

  <div class="review-list">
<% reviews.forEach(function(review) { %>
    <div class="review">
      <div class="review-head">
        <div class="reviewer"><%=review.user_name %></div>
        <div class="review-stars-row"><% for (let i = 0; i < review.rating; i++) { %>★<% } %></div>
      </div>
      <div class="review-date"><%=review.review_date %></div>
      <p class="review-body">
      </p>
        <%=review.comment %>
      </p>
      <% if (session.user && session.user.id === review.user_id) { %>
        <a href="/vehicles/<%= vehicleId %>/reviews/<%= review.id %>/edit">Edit</a>
        <form method="post" action="/vehicles/<%= vehicleId %>/reviews/<%= review.id %>?_method=DELETE" onsubmit="return confirm('Are you sure you want to delete this review?');">
          <button type="submit">Delete</button>
        </form>
      <% } %>
    </div>
<% }); %>

   
  </div>

  <!-- new row into reviews: vehicle_id, user_id, rating, comment -->
   
  <div class="review-form-panel">
    <% if (session.user) { %>
    <h3>Leave a Review</h3>
    <form id="review-form" method="post" action="<%= '/vehicles/' + vehicleId + '/reviews' %>">
      <input type="hidden" name="userId" value="<%= session.user ? session.user.id : '' %>">
      <input type="hidden" name="vehicleId" value="<%= vehicle.id %>">
      <div class="rf-field">
        <label id="rating-label">Rating</label>
        <fieldset class="star-rating-input" aria-labelledby="rating-label">
          <legend class="sr-only">Star rating</legend>
          <input type="radio" id="star5" name="rating" value="5" required><label for="star5" title="5 stars">★</label>
          <input type="radio" id="star4" name="rating" value="4"><label for="star4" title="4 stars">★</label>
          <input type="radio" id="star3" name="rating" value="3"><label for="star3" title="3 stars">★</label>
          <input type="radio" id="star2" name="rating" value="2"><label for="star2" title="2 stars">★</label>
          <input type="radio" id="star1" name="rating" value="1"><label for="star1" title="1 star">★</label>
        </fieldset>
        <div class="rating-fallback-note">Click a star to set your rating — no JavaScript needed.</div>
      </div>
      <div class="rf-field">
        <label for="rtext">Comment</label>
        <textarea id="rtext" name="comment" placeholder="Tell us how it's holding up..." required></textarea>
      </div>
      <button type="submit" class="post-review-btn">Post Review</button>
    </form>  
    <% } else { %>
    <p class="rf-field">Please <a href="/login">log in</a> to leave a review.</p>
  <% } %>
  </div>


</main>

<footer id="reviews-posted">
  Reviews reflect the opinions of individual customers, some of whom we are actively
  in mediation with. Large Motors reserves the right to respond.
</footer>

